inherit "/obj/tomes/tome.c";


void create()
{
    set_id(({"roylance", "Roylance", "guide" , "Guide"}));
    set_short("Roylance Guide to Secret Societies and Sects");
    set_long("A stapled manuscript with well worned pages.");
    setReadTime(10);
    setDiff(0);
}

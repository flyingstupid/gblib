inherit "/obj/tomes/tome.c";


void create()
{
    set_id(({"spates", "Spates", "catalog" , "Catalog", "nameless", "Nameless", "horror", "Horror"}));
    set_short("Spates Catalog of Nameless Horrors");
    set_long("A large leather bound book with strange symbols engraved on its cover. The pages are well worn, singed and stained.");
    setReadTime(10);
    setDiff(0);
}

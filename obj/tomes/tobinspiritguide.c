inherit "/obj/tomes/tome.c";


void create()
{
    set_id(({"tobin", "Tobin", "guide" , "Guide", "spirit" , "Spirit"}));
    set_short("Tobin Spirit Guide");
    set_long("A large leather bound book with a T engraved on its cover.");
    setReadTime(10);
    setDiff(0);
}
